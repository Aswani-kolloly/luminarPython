var a=1,b=1,c=3
if(a>=b&&a>=c)
{
    if(b>c)
    console.log(`${b} is the second largest num `);
    else
    console.log(`${c} is sec large`);
}
else if(b>=c&&b>=a)
{
    if(c>a)
    console.log(`${c} is the second largest num `);
    else
    console.log(`${a} is sec large`);


}